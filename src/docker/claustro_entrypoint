#!/usr/bin/env bash

# Forward the args claustro passes us (currently:
# --allow-all plus any user-supplied args) into the
# zellij layout via an env var that the layout's `bash -c` expands at
# runtime. printf ' %q' preserves arguments containing spaces.
export CLAUSTRO_COPILOT_ARGS=$(printf ' %q' "$@")

exec zellij --layout /etc/claustro/layout.kdl --config-dir /etc/claustro
